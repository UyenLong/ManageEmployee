#ifndef PROJECT_CPP
#define PROJECT_CPP

#include "project.h"

project::project(string pName, int pNumber, string pLocation, int dNumber)
{
    _pName = pName;
    _pNumber = pNumber;
    _pLocation = pLocation;
    _dNumber = dNumber;
}

#endif