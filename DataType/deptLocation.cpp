#ifndef DEPTLOCATION_CPP
#define DEPTLOCATION_CPP

#include "deptLocation.h"


deptLocation::deptLocation(string dLocation, int dNumber)
{
    _dLocation = dLocation;
    _dNumber = dNumber;
}

#endif